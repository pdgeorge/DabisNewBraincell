# Default: normalize line endings in Git to LF and check out as LF everywhere
* text=auto eol=lf

# Keep CRLF where it actually matters (Windows scripts)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.txt text eol=crlf

# Binary files: never modify line endings
*.png -text
*.jpg -text
*.gif -text
*.pdf -text
*.zip -text
*.exe -text
*.ico -text
